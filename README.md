# all_pass

This is a simple password manager using python with tkinter and sqlite3. The idea behind it is a local password manager that does not require any external connections to servers which means your passwords are always saved on your local device
